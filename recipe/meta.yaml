{% set name = "pyCGNS" %}
{% set version = "6.1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: bfd37b4942616cd9db48479ddc741b88c82fbe13ac79327ecf3d35964ae72ff4

build:
  number: 0


requirements:
  build:
    - {{ compiler('c') }}

  host:
    - python
    - pip
    - hdf5
    - numpy
    - cython
    - future
    - pkgconfig
    - qt         # [win]
    - qtpy       # [win]
    - pyqt       # [win]
    - vtk        # [win]

  run:
    - python
    - hdf5
    - {{ pin_compatible('numpy') }}
    - future
    - qt         # [win]
    - qtpy       # [win]
    - pyqt       # [win]
    - vtk        # [win]

test:
  imports:
    - CGNS.MAP
    - CGNS.PAT
    - CGNS.VAL

about:
  home: https://github.com/pyCGNS/pyCGNS
  license: LGPL-2.1-only
  license_file: LICENSE
  summary: pyCGNS provide a python interface to CGNS/SIDS datamodel for CGNS end-users and python application developpers
  doc_url: https://pycgns.github.io/
  dev_url: https://github.com/pyCGNS/pyCGNS

extra:
  recipe-maintainers:
    - MicK7
